apiVersion: v1
kind: Service
metadata:
  name: omnicraft
  labels:
    app: omnicraft
spec:
  type: ClusterIP
  selector:
    app: omnicraft
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
