apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: omnicraft

resources:
  - ../../base
  - server-config.yaml
  - gateway-networkpolicy.yaml
  - sandbox-rbac.yaml
  - sandbox-clusterrole.yaml
  - sandbox-clusterrolebinding.yaml

# Use the server image variant that includes the openshell SDK extra
# (built by CI with OMNICRAFT_EXTRAS=openshell).
images:
  - name: ghcr.io/omnicraft-ai/omnicraft-server
    newName: ghcr.io/omnicraft-ai/omnicraft-server-openshell

patches:
  - path: configmap-patch.yaml
  - path: secret-patch.yaml
  - path: deployment-patch.yaml
