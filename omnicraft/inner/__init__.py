"""Pre-integration omnicraft code. Moved here during unification.

Internal imports between files here use relative syntax (from .X import
Y). Cross-boundary imports from omnicraft-native code inside
omnicraft/ use the explicit path (omnicraft.inner.X) so the
dependency is grep-findable.
"""
